<UserControl x:Class="DD.Axis"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DD"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

        <Style x:Key="AxisSytle" TargetType="Thumb">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Rectangle Fill="{TemplateBinding Background}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Canvas >

            <Thumb x:Name="top_thumb"  DragDelta="Thumb_DragDelta" Style="{StaticResource AxisSytle}" />
            <Thumb x:Name="right_thumb"  DragDelta="Thumb_DragDelta" Style="{StaticResource AxisSytle}" />
            <Thumb x:Name="bottom_thumb"  DragDelta="Thumb_DragDelta" Style="{StaticResource AxisSytle}" />
            <Thumb x:Name="left_thumb"  DragDelta="Thumb_DragDelta" Style="{StaticResource AxisSytle}" />
        </Canvas>
    </Grid>
        
</UserControl>
